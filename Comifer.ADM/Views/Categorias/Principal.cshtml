@model List<Comifer.ADM.ViewModels.DetailedCategoryViewModel>
@{
    ViewData["Title"] = "Lista Categorias";
}

@section header {
    <div class="row align-items-center py-4">
        <div class="col-lg-6 col-7">
            <h6 class="h2 text-white d-inline-block mb-0">Categoria</h6>
            <nav aria-label="breadcrumb" class="d-none d-md-inline-block ml-md-4">
                <ol class="breadcrumb breadcrumb-links breadcrumb-dark">
                    <li class="breadcrumb-item"><a href="@Url.Action("Principal", "Inicio")"><i class="fas fa-home"></i> Inicio</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Categorias</li>
                </ol>
            </nav>
        </div>
        <div class="col-lg-6 col-5 text-right">
            <a href="@Url.Action("Incluir","Categorias")" class="btn btn-sm btn-neutral">Nova</a>
        </div>
    </div>
}

<div class="col">
    <div class="card">
        <div class="card-header border-0">
            <h3 class="mb-0">Lista</h3>
        </div>
        <div class="table-responsive">
            <table class="table align-items-center table-flush">
                <thead class="thead-light">
                    <tr>
                        <th scope="col" class="sort" data-sort="name">Nome</th>
                        <th scope="col" class="sort text-center" data-sort="productparents">Qtd. Vistas Explodidas</th>
                        <th scope="col" class="text-center" width="200px">Ações</th>
                    </tr>
                </thead>
                <tbody class="list">
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="name">
                                @item.Name
                            </td>
                            <td class="productparents text-center">
                                @item.ProductParentCount
                            </td>
                            <td class="text-right text-center">
                                <a class="btn btn-sm btn-outline-primary" href="@Url.Action("Editar", "Categorias", new { id = @item.Id })" role="button">
                                    <i class="fas fa-edit"></i> Editar
                                </a>
                                <a class="btn btn-sm btn-outline-info" href="@Url.Action("Detalhes", "Categorias", new { id = @item.Id })" role="button">
                                    <i class="fa fa-search"></i> Detalhes
                                </a>
                                <a class="btn btn-sm btn-outline-dark" href="@Url.Action("Principal", "VistasExplodidas", new { idCategoria = @item.Id })" role="button">
                                    <i class="fa fa-tools"></i> Vistas Explodidas
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
