@model Comifer.ADM.ViewModels.DetailedBrandViewModel
@{
    ViewData["Title"] = "Detalhes Marca";
}

@section header {
    <div class="row align-items-center py-4">
        <div class="col-lg-6 col-7">
            <h6 class="h2 text-white d-inline-block mb-0">Marcas</h6>
            <nav aria-label="breadcrumb" class="d-none d-md-inline-block ml-md-4">
                <ol class="breadcrumb breadcrumb-links breadcrumb-dark">
                    <li class="breadcrumb-item"><a href="@Url.Action("Principal", "Inicio")"><i class="fas fa-home"></i> Inicio</a></li>
                    <li class="breadcrumb-item"><a href="@Url.Action("Principal", "Marcas")">Marcas</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Detalhes</li>
                </ol>
            </nav>
        </div>
        <div class="col-lg-6 col-5 text-right">
            <a href="@Url.Action("Principal","Marcas")" class="btn btn-sm btn-neutral">Lista de Marcas</a>
        </div>
    </div>
}

<div class="col-xl-12 order-xl-1">
    <div class="card">
        <div class="card-header">
            <div class="row align-items-center">
                <div class="col-8">
                    <h3 class="mb-0">Detalhes</h3>
                </div>
                <div class="col-4">
                    <a href="@Url.Action("Editar", "Marcas", new { id = Model.Id })" class="btn btn-primary btn-sm pull-right">
                        <i class="fa fa-edit"></i> Editar
                    </a>
                </div>
            </div>
        </div>
        <div class="card-body detailment">
            <div class="pl-lg-12">
                <div class="form-group">
                    @Html.LabelFor(m => m.Name)
                    @Html.DisplayFor(m => m.Name)
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.SiteUrl)
                    @Html.DisplayFor(m => m.SiteUrl)
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.ProviderId)
                    @Html.DisplayFor(m => m.ProviderName)
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.ProductParentCount)
                    @Html.DisplayFor(m => m.ProductParentCount)
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.ProductCount)
                    @Html.DisplayFor(m => m.ProductCount)
                </div>
            </div>
        </div>
    </div>
</div>